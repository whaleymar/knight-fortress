package main

// make loadTexture() a method for this struct
// requestTextureSlot()
// idk other stuff
type TextureSlotManager struct {
	textures [32]uint32
}

// func (textureMgr *TextureSlotManager) getTexture()
